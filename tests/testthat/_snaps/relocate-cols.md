# with `names` given as unknown type errors gracefully

    Code
      relocate_cols(data, TRUE)
    Condition
      Error in `relocate_cols()`:
      ! `names` must be 'character' or 'integer' not 'logical'.

